# Flow Diagram

Start → upload/capture → validate type, size and resolution → valid? (no: error and retry; yes: preprocess) → model inference → confidence check → retrieve properties → apply risk/care rules → create recommendations → persist prediction → show report → end.

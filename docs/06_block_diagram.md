# Block Diagram

User → Web/camera input → image validation → preprocessing → CNN/MobileNetV2 → probabilities → fabric class → confidence → care knowledge base → recommendation engine → wash/dry/iron/bleach/eco guidance → result dashboard → SQLite history.
